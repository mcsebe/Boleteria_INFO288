1. Crear entorno virtual llamado flask con el comando "python -m venv flask" o si tiene python version 3 "python3 -m venv flask"
2. Activar el entorno virtual con el comando ".\flask\Scripts\activate"
3. Intalar requeriments.txt con el comando "pip install -r requirements.txt -v"
4. Ejecutar:
    En la terminal ejecutar el comando "flask run -p 5001"
