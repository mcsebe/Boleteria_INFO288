Crear entorno virtual llamado flask con el comando "python -m venv flask" o si tiene python version 3 "python3 -m venv flask"
Activar el entorno virtual con el comando ".\flask\Scripts\activate" en windows o en linux con ". flask/bin/activate"
Instalar requeriments.txt con el comando "pip install -r requirements.txt -v"
Ejecutar:
    En la terminal ejecutar el comando "flask run -p 5100"
    
